language: python
python:
    - "2.7"
    - "3.2"
    - "3.3"
before_install:
    - pip install pytest pytest-cov
    - pip install coveralls
install: "pip install -r requirements.txt"
script:
    - py.test
after_success:
    - coveralls